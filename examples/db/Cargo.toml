[package]
name = "db"
version = "0.1.0"
edition = "2021"

[dependencies]
cargobase = { path = "../.." }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full", "macros"] }
